from django.apps import AppConfig

class FormOciaRegConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'ocia_participant'
    verbose_name = 'OCIA Participant Form'
